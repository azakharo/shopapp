div(ng-include='"components/navbar/navbar.html"')
.container.login-container
  .row
    .col-sm-12
      .text-xlarge.mb1 Login
      p
        | There are 2 predefined accounts:
      p
        | Guest account
        code test@test.com
        | /
        code test
      p
        | Admin account
        code admin@admin.com
        | /
        code admin

    .col-sm-12
      form.form(name='form', ng-submit='login(form)', novalidate='')
        .form-group
          label Email
          input.form-control(type='email', name='email', ng-model='user.email' required='', autofocus='',
          ng-change='onFormChange()')
          p.help-block(ng-show='form.$dirty && form.email.$error.required')
            | Please specify email
          p.help-block(ng-show='form.$dirty && form.email.$error.email')
            | wrong format

        .form-group
          label Password
          input.form-control(type='password', name='password', ng-model='user.password' required='',
          ng-change='onFormChange()')
          p.help-block(ng-show='form.$dirty && form.password.$error.required')
            | Please specify password

        .form-group.has-error(ng-show="requestError")
          p.help-block {{ requestError }}

        div
          button.btn.btn-success.btn-lg.btn-login(type='submit', ng-disabled='form.$invalid || requestError')
            | Login
          = ' '
          a.btn.btn-default.btn-lg.btn-register(href='/signup')
            | Sign Up

        hr
